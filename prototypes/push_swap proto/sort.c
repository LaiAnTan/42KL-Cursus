#include "push_swap.h"

void	sortsmall(t_stack *stack)
{

}

void	sort(t_stack *stack)
{
	if (stack ->size_a <= 25)
	{
		sortsmall(stack);
	}
	else if (stack ->size_a > 25 &&stack ->size_a <= 250)
	{

	}
	else if (stack ->size_a > 250)
	{

	}
	return ;
}